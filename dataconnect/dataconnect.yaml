specVersion: "v1beta"
serviceId: "actualbackend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "actualbackend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
